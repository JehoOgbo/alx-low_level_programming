#include "main.h"

/**
 * print-number - function that prints numbers
 *
 * @n: number to be printed
 *
 * Return: void
 */
void print_number(int n)
{
	if (n < 10)
		_putchar(n);
	else if (n > 10 && n < 100)
}
